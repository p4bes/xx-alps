# xx-alps

Static route desk for the 2026 French Alps road-bike stage trip.

The app lives in `alpen_radtour/` and is deployed through GitHub Pages. The generated map, GeoJSON, GPX files, and Pages artifact are built in CI instead of being committed.

See `alpen_radtour/README.md` for local build and deployment details.
